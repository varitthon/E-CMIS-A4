# กิจกรรมที่ 10.3 — Part 5–6 Implementation Plan

**Scope:** [flow-page-05.md](flow-page-05.md) + [flow-page-06.md](flow-page-06.md) — รับคำพิพากษาศาลปกครองชั้นต้น → วิเคราะห์ผลคำพิพากษา/กำหนดแนวทาง (**LAW0119–LAW0128**, ไม่รวม LAW0116–0118 ที่เป็นขั้นนอกระบบ: ศาล/อัยการ/สารบรรณกลาง)
**Source:** `AS-IS10.3-swimlane-split.drawio` หน้า 5–6
**Status:** วางแผนเท่านั้น ยังไม่ implement — รอคำสั่ง "implement" ก่อนสร้าง/แก้ไฟล์จริง

## Confirmed decisions

| # | คำถาม | คำตอบ |
|---|---|---|
| 1 | เคสคำพิพากษามาจากไหน (Part 2–4 ยังไม่มี) | **สร้างเคสใหม่** ต่อ 1 หนังสือแจ้งผลคำพิพากษา |
| 2 | จุดรับเรื่อง LAW0119 | **ใน `02-board-intake.html`** — หมวด "คดีศาลปกครอง" เพิ่มตัวเลือกประเภทเรื่อง `รับคำฟ้อง` (Part 1 เดิม) / `รับผลคำพิพากษา` (ใหม่) |
| 3 | เชื่อมกับคดีรับคำฟ้องเดิม | **ค้นหาเชื่อมได้ (ไม่บังคับ)** — ค้นจากหมายเลขคดีดำ ถ้าเจอ copy ศาล/คู่ความ/เลขคดีดำ + เก็บลิงก์ ถ้าไม่เจอกรอกเอง |
| 4 | LAW0120 ลงทะเบียนคดีปกครอง | **ธุรการกองกฎหมาย (`admin_legal` / Kanda.R)** ทำ — **แยกหน้าลงทะเบียนต่างหาก** (intake → เข้า inbox ของ Kanda.R อีกรอบ → ลงทะเบียน → เสนอ ผอ.กอง) — ไม่เพิ่มบทบาทใหม่ |
| 5 | ขั้น (เพิ่ม) ผอ.กลุ่มงานคดี เสนอ ผอ.กอง ใน flow-page-05.md | **ตัดออก** — ลำดับเป็น ธุรการ → ผอ.กองกฎหมาย → ผอ.กลุ่มงานคดี → นิติกร |
| 6 | หน้านิติกร LAW0123/0124/0125 | **รวมเป็นหน้าเดียว** (แบบเดียวกับ `10-3-04`) |
| 7 | ผลคดี | **ชนะ / แพ้ / แพ้บางส่วน** — แพ้บางส่วนไปทางเดียวกับ "แพ้" |
| 8 | ใครเลือกทาง LAW0128 | **นิติกรเสนอ (LAW0125) → ผอ.กองกฎหมายยืนยัน/เปลี่ยน (LAW0127/0128)** |

## Flow ใหม่ (เทียบผัง AS-IS)

```
[02-board-intake, ธุรการกองกฎหมาย, LAW0119]  ประเภท = รับผลคำพิพากษา
    → L3V_PENDING_ADMIN_REGISTER
[10-3v-01, ธุรการกองกฎหมาย, LAW0120]  ลงทะเบียนคดีปกครอง
    → L3V_PENDING_DIRECTOR_ASSIGN
[10-3v-02, ผอ.กองกฎหมาย, LAW0121]  ลงนามมอบหมาย
    → L3V_PENDING_GROUP_ASSIGN
[10-3v-03, ผอ.กลุ่มงานคดี, LAW0122]  มอบหมายนิติกร
    → L3V_PENDING_LAWYER_ANALYSIS
[10-3v-04, นิติกร กลุ่มงานคดี, LAW0123+0124+0125]  ตรวจ/บันทึกผล/วิเคราะห์+ความเห็น+เสนอแนวทาง
    → L3V_PENDING_GROUP_APPROVE
[10-3v-05, ผอ.กลุ่มงานคดี, LAW0126]  พิจารณาและเห็นชอบ
    → L3V_PENDING_DIRECTOR_DECIDE
[10-3v-06, ผอ.กองกฎหมาย, LAW0127+0128]  กำหนดแนวทาง (เลือก 1 ใน 3)
    ├─ ชนะคดี + ผู้ฟ้องยื่นอุทธรณ์     → L3V_TO_APPEAL_REPLY     (black box → Part 7, LAW0129)
    ├─ แพ้คดี / แพ้บางส่วน            → L3V_TO_APPEAL_CONSIDER  (black box → Part 8, LAW0142)
    └─ ชนะคดี + ผู้ฟ้องไม่ยื่นอุทธรณ์  → L3V_TO_CLOSE            (black box → Part 10, LAW0163)
```

คำนำหน้า `L3V_` (V = verdict) แยกจาก `L3_` (Part 1) และ `L3B_` (Part 1b) ไม่ให้ชนกัน / ชื่อไฟล์ใช้ prefix `10-3v-` ตามแบบ `10-3b-`

## File plan — 6 ไฟล์ใหม่ + แก้ไข 3 ไฟล์เดิม

| # | ไฟล์ | สถานะไฟล์ | บทบาท | LAW# | ทำอะไร | `statusCode` ที่เขียน |
|---|---|---|---|---|---|---|
| 1 | `02-board-intake.html` | แก้ไข | ธุรการกองกฎหมาย | LAW0119 | เพิ่มประเภทเรื่องใต้หมวด 10.3 + ช่องข้อมูลคำพิพากษา + ค้นหาคดีเดิม | `L3V_PENDING_ADMIN_REGISTER` |
| 2 | `10-3v-01-legal-admin-register-verdict.html` | ใหม่ | ธุรการกองกฎหมาย | LAW0120 | ลงทะเบียนคดีปกครอง (แทนสมุดคุม Excel) | `L3V_PENDING_DIRECTOR_ASSIGN` |
| 3 | `10-3v-02-legal-director-assign.html` | ใหม่ | ผอ.กองกฎหมาย | LAW0121 | ลงนามมอบหมาย (sign modal แบบ `10-3-02`) | `L3V_PENDING_GROUP_ASSIGN` |
| 4 | `10-3v-03-group-director-assign.html` | ใหม่ | ผอ.กลุ่มงานคดี | LAW0122 | มอบหมายนิติกร (แบบ `10-3-03`) | `L3V_PENDING_LAWYER_ANALYSIS` |
| 5 | `10-3v-04-lawyer-verdict-analysis.html` | ใหม่ | นิติกร กลุ่มงานคดี | LAW0123–0125 | ตรวจคำพิพากษา + บันทึกผล + วิเคราะห์/บันทึกความเห็น + เสนอแนวทาง | `L3V_PENDING_GROUP_APPROVE` |
| 6 | `10-3v-05-group-director-approve.html` | ใหม่ | ผอ.กลุ่มงานคดี | LAW0126 | พิจารณาและเห็นชอบ (แบบ `10-3-07`) | `L3V_PENDING_DIRECTOR_DECIDE` |
| 7 | `10-3v-06-legal-director-decide.html` | ใหม่ | ผอ.กองกฎหมาย | LAW0127–0128 | ยืนยัน/เปลี่ยนแนวทาง + ลงนาม | 1 ใน 3 สถานะ `L3V_TO_*` |
| 8 | `assets/ecmis-10-3.js` | แก้ไข | — | — | เพิ่ม `VERDICT_STEPS`, routes, helper (ดูด้านล่าง) | — |
| 9 | `01-work-inbox.html` | แก้ไข (ถ้าจำเป็น) | — | — | เช็คว่า route/ปุ่ม "ดำเนินการ" ของ `L3V_*` ทำงานผ่าน `Activity103.ROUTES` เดิมได้เลย — แก้เฉพาะถ้าแสดงผลไม่ถูก | — |

## รายละเอียดแต่ละหน้า

### 1) `02-board-intake.html` — LAW0119 รับหนังสือแจ้งผลคำพิพากษา

- เพิ่ม `in_courtCaseType` (radio/select) ใน `div_courtCase`: `รับคำฟ้อง` (default, พฤติกรรมเดิมทุกอย่าง) / `รับผลคำพิพากษา`
- เมื่อเลือก `รับผลคำพิพากษา`:
  - **ค้นหาคดีเดิม (ไม่บังคับ):** ช่องค้นหาหมายเลขคดีดำ → ค้นในเคส 10.3 ประเภทรับคำฟ้อง → เจอแล้ว copy `courtName`, `blackCaseNo`, `plaintiffs`, `defendants` และเก็บ `l3OriginCaseId`
  - **ช่องที่แสดง:** ศาล, หมายเลขคดีดำ*, หมายเลขคดีแดง*, วันที่ศาลมีคำพิพากษา*, เลขที่หนังสือแจ้งจากสำนักงานคดีปกครอง (อัยการ)* + ลงวันที่*, วันที่ ป.ป.ท. รับเรื่อง*, ผู้ฟ้อง*/ผู้ถูกฟ้อง* (หลายราย — ใช้ component เดิม), แนบไฟล์คำพิพากษา
  - **ช่องที่ซ่อน:** ชื่อหมาย, สั่งถึง, ศาลให้ทำภายใน ... วัน (เป็นของรับคำฟ้อง)
  - เลขสารบรรณกลาง / เลขรับกองกฎหมาย ยังบังคับเหมือนเดิม
- ปุ่มยืนยันเปลี่ยนข้อความเป็น "ยืนยัน และ ส่งลงทะเบียนคดีปกครอง"
- `title` สร้างอัตโนมัติ: `ผลคำพิพากษาศาลปกครองชั้นต้น — {blackCaseNo}/{redCaseNo} ({ผู้ฟ้อง} ฟ้อง {ผู้ถูกฟ้อง})`
- routing: `status: "ธุรการลงทะเบียนคดีปกครอง"`, `statusCode: "L3V_PENDING_ADMIN_REGISTER"`, `l3CaseType: "verdict"`, `assignedRole: "admin_legal"`

### 2) `10-3v-01` — LAW0120 ลงทะเบียนคดีปกครอง (ธุรการกองกฎหมาย)

- การ์ดข้อมูลคำพิพากษา (read-only จาก intake) + ลิงก์ไปคดีเดิมถ้ามี `l3OriginCaseId`
- **เลขทะเบียนคดีปกครอง** (`l3VerdictRegNo`) — พิมพ์เองได้ หรือกดปุ่ม "ออกเลข" (แบบ `10-3-09`) + วันที่ลงทะเบียน (default วันนี้)
- หมายเหตุ (optional)
- ปุ่ม "ลงทะเบียน และ เสนอ ผอ.กองกฎหมาย"

### 3) `10-3v-02` — LAW0121 ผอ.กองกฎหมาย แจกจ่าย/มอบหมาย

- clone โครง `10-3-02-legal-director-assign.html` — เปลี่ยนการ์ดข้อมูลเป็นข้อมูลคำพิพากษา + เลขทะเบียน, ใช้ `VERDICT_STEPS` ใน stepper

### 4) `10-3v-03` — LAW0122 ผอ.กลุ่มงานคดี มอบหมายนิติกร

- clone โครง `10-3-03-group-director-assign.html`

### 5) `10-3v-04` — LAW0123+0124+0125 นิติกร (หน้ารวม)

| การ์ด | LAW | ช่อง |
|---|---|---|
| ข้อมูลคำพิพากษา | 0123 | read-only + ไฟล์คำพิพากษา + ลิงก์คดีเดิม |
| ตรวจสอบคำพิพากษา/มูลเหตุคดี | 0123 | ประเด็นแห่งคดี / มูลเหตุคดี (textarea) |
| บันทึกผลคำพิพากษา | 0124 | ผลคดี* (`ชนะ` / `แพ้` / `แพ้บางส่วน`), สรุปคำพิพากษา (textarea), วันที่อ่าน/รับทราบคำพิพากษา*, วันครบกำหนดอุทธรณ์ (auto +30 วัน แก้ได้), ถ้า `ชนะ` → ผู้ฟ้องคดียื่นอุทธรณ์?* (`ยื่น` / `ไม่ยื่น`) |
| วิเคราะห์ + บันทึกความเห็น | 0125 | บันทึกความเห็น* (textarea แบบ `10-3-04`) + แนบเอกสาร |
| แนวทางที่เสนอ | 0125 | แสดงอัตโนมัติจากผลคดี (read-only badge): ชนะ+ยื่น → "จัดทำคำแก้อุทธรณ์", แพ้/แพ้บางส่วน → "พิจารณาความเห็นควรอุทธรณ์", ชนะ+ไม่ยื่น → "ยุติ/ปิดสำนวน" |

เขียนลงเคส: `l3VerdictResult`, `l3AppealByPlaintiff`, `l3VerdictReadDate`, `l3AppealDeadline`, `l3VerdictIssues`, `l3VerdictSummary`, `l3VerdictOpinionText`, `l3VerdictOpinionAttachments`, `l3ProposedBranch` (`APPEAL_REPLY` / `APPEAL_CONSIDER` / `CLOSE`)

### 6) `10-3v-05` — LAW0126 ผอ.กลุ่มงานคดี พิจารณาและเห็นชอบ

- clone โครง `10-3-07-group-director-approve.html` — แสดงผลคดี + ความเห็นนิติกร + แนวทางที่เสนอ

### 7) `10-3v-06` — LAW0127+0128 ผอ.กองกฎหมาย กำหนดแนวทาง

- แสดงข้อมูลทั้งหมด + ความเห็นนิติกร + ผลการเห็นชอบของ ผอ.กลุ่มงาน
- radio 3 ทาง **เลือกไว้ล่วงหน้าตาม `l3ProposedBranch`**; ถ้าเปลี่ยนต่างจากที่เสนอ → บังคับกรอกเหตุผล (`l3DirectorBranchReason`)
- ลงนาม (sign modal) → `Activity103.advance()` ด้วย patch override `status`/`statusCode` ตามทางที่เลือก:

| ทาง | `statusCode` | `status` |
|---|---|---|
| ชนะคดี (ผู้ฟ้องคดียื่นอุทธรณ์) | `L3V_TO_APPEAL_REPLY` | รอจัดทำคำแก้อุทธรณ์ (กิจกรรมที่ 7) |
| แพ้คดี | `L3V_TO_APPEAL_CONSIDER` | รอพิจารณาความเห็นควรอุทธรณ์ (กิจกรรมที่ 8) |
| ชนะคดี (ผู้ฟ้องคดีไม่ยื่นอุทธรณ์) | `L3V_TO_CLOSE` | รอปิดสำนวน (LAW0163) |

## การแก้ `assets/ecmis-10-3.js`

- เพิ่ม `VERDICT_STEPS` (6 entries: 10-3v-01 … 10-3v-06) แล้วรวมเข้า `ALL_STEPS` → `stepByCode`/`stepByPage` ใช้ได้ทันที
- `ROUTES["L3V_PENDING_ADMIN_REGISTER"] = VERDICT_STEPS[0].page` + reduce ไล่ต่อแบบ `STAY_STEPS` (3 สถานะ `L3V_TO_*` ไม่มี route — black box)
- `Activity103.isVerdictCase(kase)` → `kase.l3CaseType === "verdict"`
- `Activity103.VERDICT_BRANCHES` — ตาราง 3 ทางข้างบน (ใช้ร่วมกันใน 10-3v-04 และ 10-3v-06)
- `Activity103.findComplaintCaseByBlackNo(q)` — ใช้ในช่องค้นหาคดีเดิมที่ intake
- **ใช้ `l3OriginCaseId` ไม่ใช่ `l3ParentCaseId`** — เพราะ `isStayObjectionCase()` เช็คจาก `l3ParentCaseId` ถ้าใช้ชื่อเดียวกันเคสคำพิพากษาจะถูกนับเป็นเคสลูกคำขอทุเลาฯ ผิด

## Verification plan (หลัง implement)

1. `node --check` สำหรับ `ecmis-10-3.js` + ตรวจ div-balance ทุกหน้าที่แก้/สร้าง (E-CMIS-A4 ดูได้แค่ static ใน Browser pane)
2. Kanda.R: `02-board-intake` → หมวดคดีศาลปกครอง → เลือก "รับผลคำพิพากษา" → เช็คช่องสลับถูก, ค้นหาคดีเดิมเจอ/ไม่เจอ → ยืนยัน → เคสใหม่เข้า inbox ของ Kanda.R ที่ `L3V_PENDING_ADMIN_REGISTER`
3. เลือก "รับคำฟ้อง" → พฤติกรรม Part 1 เดิมไม่เปลี่ยน (ยังไป `L3_PENDING_DIRECTOR_ASSIGN`)
4. เดินเคสผ่าน 10-3v-01 → 02 → 03 → 04 → 05 → 06 ด้วย login ตามบทบาท
5. ที่ 10-3v-06 ทดสอบทั้ง 3 ทาง (สร้าง 3 เคส) + ทดสอบเปลี่ยนทางต่างจากที่เสนอ → ต้องบังคับเหตุผล
6. เช็คว่าเคสคำพิพากษาไม่ถูกนับเป็นเคสลูก Part 1b และ guardRole warning ขึ้นเมื่อบทบาทไม่ตรง

---

## ส่วนขยาย — ชนะคดี (ผู้ฟ้องไม่ยื่นอุทธรณ์) → ปิดสำนวน (LAW0163)

**Status:** วางแผนเท่านั้น ยังไม่ implement — รอคำสั่ง "implement" ก่อนสร้างไฟล์จริง
**Scope:** [flow-page-10.md](flow-page-10.md) เฉพาะโหนด LAW0163 (จุดเข้าร่วมจาก [flow-page-06.md](flow-page-06.md) Part 6c — เงื่อนไข "ชนะคดี (ผู้ฟ้องคดีไม่ยื่นอุทธรณ์)")

หลัง `10-3v-06` เขียนสถานะ `L3V_TO_CLOSE` (ผอ.กองกฎหมายลงนามเลือกทาง "ชนะคดี (ไม่มีผู้ยื่นอุทธรณ์)") เคสจะจบด้วยขั้นตอนเดียว — LAW0163 "ดำเนินการตามคำพิพากษา (ถ้ามี)/บันทึกคดีถึงที่สุด/ปิดสำนวน" โดยนิติกร กลุ่มงานคดี — เป็นจุดปิดสำนวนร่วมกับสายอุทธรณ์ (LAW0163 ซ้ำใน Page 10/11 ด้วย แต่ที่นี่ implement เฉพาะทางเข้าจาก Part 6c)

### Flow

```
10-3v-06 (ชนะคดี + ไม่ยื่นอุทธรณ์, ลงนามแล้ว)
    → L3V_TO_CLOSE
[10-3v-07, นิติกร, LAW0163]  ดำเนินการตามคำพิพากษา / บันทึกคดีถึงที่สุด / ปิดสำนวน
    → L3V_CLOSED  (จบ — terminal, ไม่มี route ต่อ เหมือน L3B_CLOSED ของ Part 1b)
```

### File plan — 1 ไฟล์ใหม่ + แก้ไข 1 ไฟล์เดิม

| # | ไฟล์ | สถานะไฟล์ | บทบาท | LAW# | ทำอะไร | `statusCode` ที่เขียน |
|---|---|---|---|---|---|---|
| 1 | `10-3v-07-lawyer-close-case.html` | ใหม่ | นิติกร กลุ่มงานคดี | LAW0163 | ดำเนินการตามคำพิพากษา (ถ้ามี) + บันทึกวันที่คดีถึงที่สุด + ปิดสำนวน | `L3V_CLOSED` *(terminal)* |
| 2 | `assets/ecmis-10-3.js` | แก้ไข | — | — | เพิ่ม `VERDICT_STEPS` entry ที่ 7 (LAW0163), เพิ่ม `ROUTES["L3V_TO_CLOSE"]` ชี้ไป `10-3v-07-lawyer-close-case.html`, เพิ่มชื่อไฟล์ใน `VERDICT_BUILT_PAGES` | — |

### รายละเอียดหน้า `10-3v-07`

**การ์ด 1 — ข้อมูลคำพิพากษา (อ่านอย่างเดียว):** ศาล, วันที่พิพากษา, คดีดำ/คดีแดง, ผู้ฟ้อง/ผู้ถูกฟ้อง, ผลคดี ("ชนะคดี"), แนวทางที่ ผอ.กองกฎหมายกำหนด ("ชนะคดี (ผู้ฟ้องคดีไม่ยื่นอุทธรณ์) → ยุติ/ปิดสำนวน"), ลิงก์คดีเดิมถ้ามี `l3OriginCaseId`

**การ์ด 2 — ดำเนินการตามคำพิพากษา / ปิดสำนวน (นิติกร):**

| ฟิลด์ | ประเภท | บังคับ |
|---|---|---|
| วันที่คดีถึงที่สุด | date (default วันนี้) | * |
| การดำเนินการตามคำพิพากษา (ถ้ามี) | textarea — เช่น การชดใช้ค่าเสียหาย, การเพิกถอนคำสั่ง ฯลฯ | ไม่บังคับ |
| หมายเหตุปิดสำนวน | textarea | ไม่บังคับ |
| แนบเอกสารปิดสำนวน | ไฟล์ (หลายไฟล์ได้) | ไม่บังคับ |

ปุ่ม **"ยืนยันปิดสำนวน"** → confirm ธรรมดา (ไม่ลงนาม — เป็นงานบันทึกข้อมูล ไม่ใช่การลงนามเอกสารทางกฎหมาย) → `Activity103.advance()` เขียน:
`l3CaseFinalDate`, `l3CloseAction`, `l3CloseNotes`, `l3CloseAttachments`, `status: "ปิดสำนวนคดีแล้ว"`, `statusCode: "L3V_CLOSED"`

### การแก้ `assets/ecmis-10-3.js`

- เพิ่ม `VERDICT_STEPS[6]` (code `LAW0163`, page `10-3v-07-lawyer-close-case.html`, role `case_legal_officer`, statusCode `L3V_CLOSED`) — แต่**ไม่ใช้ reduce อัตโนมัติ**เพราะ entry point ของขั้นนี้คือ `L3V_TO_CLOSE` (เขียนโดย `VERDICT_BRANCHES.CLOSE`) ไม่ใช่ statusCode ของ `VERDICT_STEPS[5]` (LAW0127) — ต้องเพิ่มบรรทัด `ROUTES["L3V_TO_CLOSE"] = "10-3v-07-lawyer-close-case.html"` ด้วยมือ (แบบเดียวกับที่ `L3B_PENDING_LAWYER_DRAFT`/เดิม `L3V_PENDING_ADMIN_REGISTER` เคยทำ) พร้อม gate ด้วย `VERDICT_BUILT_PAGES`
- `L3V_TO_APPEAL_REPLY`/`L3V_TO_APPEAL_CONSIDER` ยังไม่มี route (รอ Part 7/8) — ไม่แตะ

### Verification plan (หลัง implement)

1. `node --check` + div-balance ของไฟล์ใหม่/แก้ไข
2. เดินเคสครบ `10-3v-00 → 02 → 03 → 04` (เลือกผลคดี = ชนะ, ผู้ฟ้องยื่นอุทธรณ์ = ไม่ยื่น) `→ 05 → 06` (เลือกทาง "ชนะคดี (ไม่มีผู้ยื่นอุทธรณ์)") → เช็คว่า inbox โชว์ปุ่ม "ดำเนินการ" สำหรับ Kittisak.S แทน "ยังไม่ implement"
3. `10-3v-07`: กรอกวันที่คดีถึงที่สุด, ปิดสำนวน → เช็คสถานะสุดท้ายเป็น `L3V_CLOSED` และ inbox ไม่มีปุ่มดำเนินการต่อ (terminal)
4. เช็คว่า Part 1/1b/5/6 อื่นไม่กระทบ (routing simulation เหมือนทุกครั้งที่ผ่านมา)

---

## Open items (ใช้ค่าเริ่มต้นนี้ เว้นแต่จะแจ้งเปลี่ยน)

- **ชนะคดีแต่ยังไม่ทราบว่าผู้ฟ้องจะอุทธรณ์ไหม** (ยังไม่ครบ 30 วัน) — ค่าเริ่มต้น: มีแค่ `ยื่น`/`ไม่ยื่น` ไม่มี "ยังไม่ทราบ"
- **บันทึกความเห็นใน 10-3v-04** — ค่าเริ่มต้น textarea แบบ `10-3-04` (ไม่ใช่ TipTap + live preview แบบ `10-3b-01`)
- **ปุ่มส่งกลับแก้ไข** ที่ 10-3v-05 / 10-3v-06 — ค่าเริ่มต้น: ทำตามที่ `10-3-07`/`10-3-08` มีอยู่ (ไม่เพิ่มเอง)
- **ตัวเลขสรุปใน inbox** — เคสคำพิพากษา `category: "10.3"` จะนับรวมใน "คดีศาลปกครอง" (tradeoff เดียวกับ Part 1b)
- **ทางเข้าจาก Part 4 (LAW0115 → LAW0124)** — ยังไม่ทำ เพราะ Part 4 ยังไม่มี
- **อัปเดต `task-10-3-checklist.md` + `flow-page-05.md`** (ตัดขั้น (เพิ่ม), LAW0120 เปลี่ยนเป็นธุรการ) — ทำตอน implement
